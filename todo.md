
    2. if limits are full and user tries to do the same task again, we can show a message like "You've reached your monthly limit for paper generation. Please upgrade to continue generating more papers." and disable the generate button until the next month or until they upgrade or its limit resets.

    4. and one more thing if limits are exceed on this /generate route, dont disable the button but if limits are exhausted on click of    
   it show a toast saying limit exceed, upgrade to continue   
   
    5. show realtime metrics on dashboard like if the paper is being generated so show it as being generated and if its generated show it as generated and if its failed show it as failed, and also show the number of questions generated in that paper and the number of resources used for that paper, and also show the quota left for that user for that month in the dashboard. (not exactly realtime but if the task is being processed show a loading spinner or something )

    clicked export study guide but both buttons got disabled 

    was able to generate paper with 0 0 0 values for exam pattern